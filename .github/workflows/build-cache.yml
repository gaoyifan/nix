name: Build and Push Binary Cache

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

env:
  AWS_ACCESS_KEY_ID: ${{ secrets.R2_ACCESS_KEY_ID }}
  AWS_SECRET_ACCESS_KEY: ${{ secrets.R2_SECRET_ACCESS_KEY }}
  BINARY_CACHE_SECRET_KEY: ${{ secrets.BINARY_CACHE_SECRET_KEY }}
  S3_ENDPOINT: ${{ secrets.S3_ENDPOINT }}

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            system: x86_64-linux
          - os: ubuntu-24.04-arm
            system: aarch64-linux
          - os: macos-latest
            system: aarch64-darwin

    runs-on: ${{ matrix.os }}
    name: Build (${{ matrix.system }})

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install Nix
        uses: cachix/install-nix-action@v30
        with:
          nix_path: nixpkgs=channel:nixos-unstable

      - name: Setup Magic Nix Cache
        uses: DeterminateSystems/magic-nix-cache-action@main

      - name: Build all custom packages
        run: nix build ".#legacyPackages.${{ matrix.system }}.customPkgs.all"

      - name: Sign package with secret key
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        run: |
          echo "$BINARY_CACHE_SECRET_KEY" > ./secret.key
          nix store sign --recursive --key-file ./secret.key ./result
          rm ./secret.key

      - name: Copy package to R2
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        run: |
          nix copy --to "s3://nix-cache?endpoint=$S3_ENDPOINT&compression=zstd" ./result
