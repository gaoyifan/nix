name: Build and Cache

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:
    inputs:
      publish-cache:
        description: "Sign and push closures to the R2 binary cache when running on main"
        required: false
        default: true
        type: boolean
      prune:
        description: "After building, delete R2 objects not referenced by the freshly built closures"
        required: false
        default: false
        type: boolean

env:
  AWS_ACCESS_KEY_ID: ${{ secrets.R2_ACCESS_KEY_ID }}
  AWS_SECRET_ACCESS_KEY: ${{ secrets.R2_SECRET_ACCESS_KEY }}
  BINARY_CACHE_SECRET_KEY: ${{ secrets.BINARY_CACHE_SECRET_KEY }}
  S3_ENDPOINT: ${{ secrets.S3_ENDPOINT }}

jobs:
  build:
    name: Build (${{ matrix.system }})
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            system: x86_64-linux
            target: homeConfigurations.yifan.config.home.activationPackage
          - os: ubuntu-24.04-arm
            system: aarch64-linux
            target: homeConfigurations.yifan.config.home.activationPackage
          - os: macos-latest
            system: aarch64-darwin
            target: darwinConfigurations.default.system

    runs-on: ${{ matrix.os }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v7

      - name: Install Nix
        uses: nixbuild/nix-quick-install-action@v35
        with:
          nix_conf: |
            keep-env-derivations = true

      - name: Restore and save Nix store
        uses: nix-community/cache-nix-action@v7.0.2
        with:
          primary-key: nix-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('**/*.nix', '**/flake.lock') }}
          restore-prefixes-first-match: nix-${{ runner.os }}-${{ runner.arch }}-
          purge: true
          purge-prefixes: nix-${{ runner.os }}-${{ runner.arch }}-

      - name: Build configuration
        run: nix build --accept-flake-config .#${{ matrix.target }}

      - name: Build devshell
        run: nix build --accept-flake-config .#devShells.${{ matrix.system }}.default -o result-devshell

      - name: Initialize cache path list
        run: |
          : > cache-store-paths

      - name: Build CLI apps
        run: |
          set -euo pipefail

          cli_apps=(
            .#packages.${{ matrix.system }}.agy
            .#packages.${{ matrix.system }}.copilot
            .#packages.${{ matrix.system }}.codex
            .#packages.${{ matrix.system }}.cursor-agent
          )

          nix build --accept-flake-config --no-link --print-out-paths \
            "${cli_apps[@]}" \
            | tee -a cache-store-paths

      - name: Build system-manager configurations
        if: runner.os == 'Linux'
        run: |
          set -euo pipefail

          system_manager_configs=(
            .#systemConfigs.${{ matrix.system }}.default
            .#systemConfigs.${{ matrix.system }}.el2
          )

          nix build --accept-flake-config --no-link --print-out-paths \
            "${system_manager_configs[@]}" \
            | tee -a cache-store-paths

      - name: Switch home-manager configuration
        if: runner.os == 'Linux'
        run: |
          # Validate the same Home Manager switch path used locally. Skip Darwin
          # because `just darwin` is too slow on GitHub-hosted runners.
          nix develop --accept-flake-config .#devShells.${{ matrix.system }}.default -c just home

      - name: Sign closure with secret key
        if: github.ref == 'refs/heads/main' && (github.event_name == 'push' || (github.event_name == 'workflow_dispatch' && inputs['publish-cache']))
        run: |
          echo "$BINARY_CACHE_SECRET_KEY" > ./secret.key

          xargs nix store sign --recursive --key-file ./secret.key ./result ./result-devshell < cache-store-paths

      - name: Push closure to R2
        if: github.ref == 'refs/heads/main' && (github.event_name == 'push' || (github.event_name == 'workflow_dispatch' && inputs['publish-cache']))
        run: |
          xargs nix copy --to "s3://nix-cache?endpoint=$S3_ENDPOINT&compression=zstd" ./result ./result-devshell < cache-store-paths

      # Record the full recursive closure that was just pushed so the prune job
      # can compute the union of objects that must be kept in R2.
      - name: Record built closure paths
        if: github.ref == 'refs/heads/main' && github.event_name == 'workflow_dispatch' && inputs['prune']
        run: |
          set -euo pipefail
          { echo ./result; echo ./result-devshell; cat cache-store-paths; } \
            | xargs nix path-info --recursive \
            | sort -u > "closure-${{ matrix.system }}.txt"
          echo "closure paths: $(wc -l < "closure-${{ matrix.system }}.txt")"

      - name: Upload closure path list
        if: github.ref == 'refs/heads/main' && github.event_name == 'workflow_dispatch' && inputs['prune']
        uses: actions/upload-artifact@v4
        with:
          name: closure-${{ matrix.system }}
          path: closure-${{ matrix.system }}.txt
          retention-days: 1

  # Garbage-collect R2: keep every object referenced by the freshly built
  # closures (across all platforms) and delete only the orphans. This replaces
  # the old "empty the whole bucket then rebuild" strategy, which transiently
  # orphaned live .narinfo pointers and forced clients to build from source.
  prune-cache:
    name: Prune orphaned R2 objects
    needs: build
    if: github.ref == 'refs/heads/main' && github.event_name == 'workflow_dispatch' && inputs['prune']
    runs-on: ubuntu-latest
    env:
      AWS_ACCESS_KEY_ID: ${{ secrets.R2_ACCESS_KEY_ID }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.R2_SECRET_ACCESS_KEY }}
      AWS_DEFAULT_REGION: auto
      S3_ENDPOINT: ${{ secrets.S3_ENDPOINT }}
      BUCKET: nix-cache
      CACHE_URL: https://nix-cache.yfgao.net
      LC_ALL: C

    steps:
      - name: Download closure path lists
        uses: actions/download-artifact@v4
        with:
          pattern: closure-*
          path: closures
          merge-multiple: true

      - name: Compute keep set (narinfos + nars)
        run: |
          set -euo pipefail
          cat closures/closure-*.txt | sort -u > all-paths.txt
          test -s all-paths.txt
          # store path -> "<hash>.narinfo"
          sed -E 's#.*/##; s/-.*//' all-paths.txt | sort -u | sed 's/$/.narinfo/' > keep-narinfos.txt
          # resolve the "nar/<filehash>.nar.zst" key referenced by each narinfo.
          # Each parallel worker prints a single short line so concurrent writes
          # to the pipe stay atomic.
          # shellcheck disable=SC2016  # $CACHE_URL/$1 expand inside the inner sh -c, by design
          xargs -P 32 -I{} sh -c 'curl -fsS "$CACHE_URL/$1" | sed -n "s/^URL: //p"' _ {} \
            < keep-narinfos.txt | sort -u > keep-nars.txt
          { cat keep-narinfos.txt keep-nars.txt; echo "nix-cache-info"; } | sort -u > keep.txt
          echo "keep objects: $(wc -l < keep.txt) (narinfos=$(wc -l < keep-narinfos.txt), nars=$(wc -l < keep-nars.txt))"

      - name: List all R2 objects
        run: |
          set -euo pipefail
          ep="$S3_ENDPOINT"; case "$ep" in http*) ;; *) ep="https://$ep";; esac
          aws s3api list-objects-v2 --endpoint-url "$ep" --bucket "$BUCKET" \
            --query 'Contents[].Key' --output text \
            | tr '\t' '\n' | sed '/^$/d' | sort -u > all-keys.txt
          echo "total R2 objects: $(wc -l < all-keys.txt)"

      - name: Delete orphaned objects
        run: |
          set -euo pipefail
          keep_count=$(wc -l < keep.txt)
          # Safety: never prune if the keep set is implausibly small (e.g. a
          # failed/empty artifact download), which would wipe the live cache.
          if [ "$keep_count" -lt 1000 ]; then
            echo "Refusing to prune: keep set suspiciously small ($keep_count objects)"
            exit 1
          fi
          comm -23 all-keys.txt keep.txt > orphans.txt
          echo "orphans to delete: $(wc -l < orphans.txt)"
          if [ ! -s orphans.txt ]; then
            echo "Nothing to prune."
            exit 0
          fi
          ep="$S3_ENDPOINT"; case "$ep" in http*) ;; *) ep="https://$ep";; esac
          split -l 1000 orphans.txt orphan-batch-
          for f in orphan-batch-*; do
            jq -Rn '{Objects: [inputs | {Key: .}], Quiet: true}' < "$f" > del.json
            aws s3api delete-objects --endpoint-url "$ep" --bucket "$BUCKET" --delete file://del.json >/dev/null
          done
          echo "Prune complete: removed $(wc -l < orphans.txt) objects."
